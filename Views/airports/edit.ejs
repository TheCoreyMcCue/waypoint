<% layout('layouts/boilerplate') %>
<h1>Edit an airport</h1>
<form action="/airports/<%= airport._id %>?_method=PUT" method="POST">
    <div>
        <label for="name">Airport Name</label>
        <input type="text" name="airport[name]" id="name" placeholder="Thun Field" value="<%= airport.name %>">
    </div>
    <div>
        <label for="location">Airport location</label>
        <input type="text" name="airport[location]" id="location" placeholder="" value="<%= airport.location %>">
    </div>
    <button>Edit Airport</button>
</form>

<p><a href="/airports/<%= airport._id %>">Back to airport</a></p>
